name: line-counter
on:	push
jobs: 
    line-counter:
        runs-on: ubuntu-latest
        steps:
          - name: calling remote action
            uses: SaverioCode/test1@v1
            with:
                who-to-greet: 'nessuno'
